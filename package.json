{
  "name": "state-managers-comparison",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/domain && tsc --noEmit -p packages/ui && tsc --noEmit -p packages/bench && tsc --noEmit -p apps/zustand && tsc --noEmit -p apps/rxjs && tsc --noEmit -p apps/mobx && tsc --noEmit -p apps/jotai && tsc --noEmit -p apps/redux && tsc --noEmit -p e2e",
    "build:apps": "npm run build --workspaces --if-present",
    "test:e2e": "npm run build:apps && playwright test e2e/parity.spec.ts e2e/cross-app-parity.spec.ts",
    "bench": "npm run build:apps && playwright test e2e/benchmark.spec.ts",
    "metrics": "node scripts/metrics.mjs",
    "report": "vite-node scripts/report.ts",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.0",
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^22.10.0",
    "@vitejs/plugin-react": "^5.2.0",
    "eslint": "^10.8.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "jsdom": "^29.1.1",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.65.0",
    "vitest": "^3.0.0"
  }
}
